# Simple test circuit - BUGGY implementation
# Bug: second gate is AND instead of OR
# Computes: f = (a AND b) AND c  (should be OR)

.model buggy
.inputs a b c
.outputs f

# n1 = a AND b (correct)
.names a b n1
11 1

# f = n1 AND c (BUG: should be OR)
.names n1 c f
11 1

.end